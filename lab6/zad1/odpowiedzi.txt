# zadanie1
   # uruchamia plik docker-compoer.yml, tworzy obrazy albo uzywa stworzonych, tworzy kontenery
   # albo je uruchamia
   docker-compose up

   # wszystko co wyzej ale nie pokazuje procesu
   docker-compose up -d

   # zatrzymuje powstale w procesie wyżej kontenery
   docker-compose stop

   # usuń powstale kontenery wraz z volumami (nie usuwa obrazów)
   docker-compose down --volumes
